package J3_Operators_Maths;

public class M6_GCD_LCM {
}
// (a+b)%m = ((a%m)+(b%m)) %m
// (a-b)%m = ((a%m)-(b%m) +m) %m
// (a*b)%m = ((a%m)*(b%m)) %m
// (a/b)%m = ((a%m)*(b^-1 %m)) %m
// b^-1 %m -> Multiplicative modulo inverse(MMI), b&m are Co-prime
// (a%m)%m = a%m
// m^x %m = 0  ∀ x ∈ +ve Integers
// If p is prime no which is not a divisor of b,
// then ab^p-1 %p =a%p due to format' little theorem

